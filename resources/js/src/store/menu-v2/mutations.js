export default {
  setAllMenu: (state, menuList) => {
    state.menuList = menuList
  },
  setAllSubMenu: (state, subMenuList) => {
    state.subMenuList = subMenuList
  },
  setAllView: (state, viewList) => {
    state.viewList = viewList
  }
}
